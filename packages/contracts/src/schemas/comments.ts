// TODO: no comment contracts exist yet.
// POST /api/posts/:postId/comment is unvalidated (createComment returns a raw
// { comment } payload), so there is nothing to extract here. Add the
// CreateComment response schemas alongside the validation when it lands.
export {};
