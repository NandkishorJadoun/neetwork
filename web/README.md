TODAY TASKS:

- [x] create a route to show the user who liked the post using posts/:postId/likes
- [x] all liked post tab on profile (first create the route in backend) then client
- [ ] create the cards for profile post, comment and likes section
- [ ] give alt to all images
- [ ] fix the src problem in image and add a default avatar image in schema from utils

LATER:

- [ ] show comments of post using hash, use commentform hash for comment form
- [ ] use state instead of hash for comment maybe